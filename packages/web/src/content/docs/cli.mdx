---
title: CLI
description: Ottili Coder CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Ottili Coder CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
ottili-coder
```

But it also accepts commands as documented on this page. This allows you to interact with Ottili Coder programmatically.

```bash
ottili-coder run "Explain how closures work in JavaScript"
```

---

### tui

Start the Ottili Coder terminal user interface.

```bash
ottili-coder [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Ottili Coder CLI also has the following commands.

---

### agent

Manage agents for Ottili Coder.

```bash
ottili-coder agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
ottili-coder agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.ottili-coder/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
ottili-coder agent list
```

---

### attach

Attach a terminal to an already running Ottili Coder backend server started via `serve` or `web` commands.

```bash
ottili-coder attach [url]
```

This allows using the TUI with a remote Ottili Coder backend. For example:

```bash
# Start the backend server for web/mobile access
ottili-coder web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
ottili-coder attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `OTTILI_CODER_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `OTTILI_CODER_SERVER_USERNAME` or `ottili-coder`) |

---

### auth

Command to manage credentials and login for providers.

```bash
ottili-coder auth [command]
```

---

#### login

Ottili Coder is powered by the provider list at [Models.dev](https://models.dev), so you can use `ottili-coder auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/ottili-coder/auth.json`.

```bash
ottili-coder auth login
```

When Ottili Coder starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
ottili-coder auth list
```

Or the short version.

```bash
ottili-coder auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
ottili-coder auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
ottili-coder github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
ottili-coder github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
ottili-coder github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
ottili-coder mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
ottili-coder mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
ottili-coder mcp list
```

Or use the short version.

```bash
ottili-coder mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
ottili-coder mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
ottili-coder mcp auth list
```

Or use the short version.

```bash
ottili-coder mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
ottili-coder mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
ottili-coder mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
ottili-coder models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
ottili-coder models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Ottili Coder.

```bash
ottili-coder models --refresh
```

---

### run

Run ottili-coder in non-interactive mode by passing a prompt directly.

```bash
ottili-coder run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "ottili-coder run"
ottili-coder run Explain the use of context in Go
```

You can also attach to a running `ottili-coder serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
ottili-coder serve

# In another terminal, run commands that attach to it
ottili-coder run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                                         | Short | Description                                                                |
| ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>                      |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr>                     | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>                      | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>                         |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>                        |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>                        | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>                        |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>                         | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>                       |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>                        |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>                       |       | Attach to a running ottili-coder server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr>                     | `-p`  | Basic auth password (defaults to `OTTILI_CODER_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr>                     | `-u`  | Basic auth username (defaults to `OTTILI_CODER_SERVER_USERNAME` or `ottili-coder`) |
| <nobr><code>{"--dir"}</code></nobr>                          |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>                         |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>                      |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr>                     |       | Show thinking blocks                                                       |
| <nobr><code>{"--dangerously-skip-permissions"}</code></nobr> |       | Auto-approve permissions that are not explicitly denied (dangerous!)       |

---

### serve

Start a headless Ottili Coder server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
ottili-coder serve
```

This starts an HTTP server that provides API access to ottili-coder functionality without the TUI interface. Set `OTTILI_CODER_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `ottili-coder`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Ottili Coder sessions.

```bash
ottili-coder session [command]
```

---

#### list

List all Ottili Coder sessions.

```bash
ottili-coder session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Ottili Coder session.

```bash
ottili-coder session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Ottili Coder sessions.

```bash
ottili-coder stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
ottili-coder export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Ottili Coder share URL.

```bash
ottili-coder import <file>
```

You can import from a local file or an Ottili Coder share URL.

```bash
ottili-coder import session.json
ottili-coder import https://opncd.ai/s/abc123
```

---

### web

Start a headless Ottili Coder server with a web interface.

```bash
ottili-coder web
```

This starts an HTTP server and opens a web browser to access Ottili Coder through a web interface. Set `OTTILI_CODER_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `ottili-coder`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
ottili-coder acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
ottili-coder plugin <module>
```

Or use the alias.

```bash
ottili-coder plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Ottili Coder.

```bash
ottili-coder pr <number>
```

---

### db

Database tools.

```bash
ottili-coder db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
ottili-coder db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
ottili-coder debug [command]
```

---

### uninstall

Uninstall Ottili Coder and remove all related files.

```bash
ottili-coder uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates ottili-coder to the latest version or a specific version.

```bash
ottili-coder upgrade [target]
```

To upgrade to the latest version.

```bash
ottili-coder upgrade
```

To upgrade to a specific version.

```bash
ottili-coder upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The ottili-coder CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Ottili Coder can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `OTTILI_CODER_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `OTTILI_CODER_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `OTTILI_CODER_CONFIG`                     | string  | Path to config file                               |
| `OTTILI_CODER_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `OTTILI_CODER_CONFIG_DIR`                 | string  | Path to config directory                          |
| `OTTILI_CODER_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `OTTILI_CODER_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `OTTILI_CODER_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `OTTILI_CODER_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `OTTILI_CODER_PERMISSION`                 | string  | Inlined json permissions config                   |
| `OTTILI_CODER_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `OTTILI_CODER_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `OTTILI_CODER_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `OTTILI_CODER_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `OTTILI_CODER_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `OTTILI_CODER_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `OTTILI_CODER_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `OTTILI_CODER_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `OTTILI_CODER_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `OTTILI_CODER_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `OTTILI_CODER_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `OTTILI_CODER_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `OTTILI_CODER_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `OTTILI_CODER_SERVER_USERNAME`            | string  | Override basic auth username (default `ottili-coder`) |
| `OTTILI_CODER_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `OTTILI_CODER_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `OTTILI_CODER_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `OTTILI_CODER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `OTTILI_CODER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `OTTILI_CODER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `OTTILI_CODER_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `OTTILI_CODER_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `OTTILI_CODER_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `OTTILI_CODER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `OTTILI_CODER_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `OTTILI_CODER_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `OTTILI_CODER_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `OTTILI_CODER_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `OTTILI_CODER_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `OTTILI_CODER_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `OTTILI_CODER_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `OTTILI_CODER_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `OTTILI_CODER_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
